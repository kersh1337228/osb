FROM node:22-slim

WORKDIR /usr/src/app

ENV PATH /usr/src/app/node_modules/.bin:$PATH

COPY package*.json ./
RUN npm ci
RUN npx next telemetry disable

COPY . .
RUN npm run build

CMD ["npm", "run", "start"]
